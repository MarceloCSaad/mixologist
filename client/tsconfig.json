{
    "files": [],
    "references": [
        { "path": "./tsconfig.app.json" },
        { "path": "./tsconfig.node.json" }
    ],
    "compilerOptions": {
        "typeRoots": ["./node_modules/@types", "./node_modules"], // Ensure these paths are included
        "types": ["node", "estree"] // Add 'estree' if it's a global type
    }
}
